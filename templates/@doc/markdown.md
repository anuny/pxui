# markdown解析 - Markdown
markdown解析,无依赖,模块大小 ≈ `2kb`
## 使用组件
``` javascript
var markdown = pxui.require('plugin::markdown')

var source = '#test'
var html = markdown(source);

```


*test*

**test**